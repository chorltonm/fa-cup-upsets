CREATE OR REPLACE TABLE `birkbeck-msc-project-422917.extract_layer.all_api_sportdevs_fb_arenas` 

(
`dimensions` STRING,
    `built` STRING,
    `geolocation` RECORD,
    `city` STRING,
    `country_name` STRING,
    `cost` STRING,
    `id` INT64,
    `opened` STRING,
    `hash_image` STRING,
    `country_hash_image` STRING,
    `stadium_capacity` INT64,
    `country_id` INT64,
    `surface` STRING,
    `owner` STRING,
    `operator` STRING,
    `name` STRING
)
