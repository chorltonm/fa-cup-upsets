CREATE OR REPLACE TABLE `birkbeck-msc-project-422917.extract_layer.all_api_sportdevs_fb_matches` 

(
`specific_start_time` TIMESTAMP,
    `end_time` TIMESTAMP,
    `referee_id` INT64,
    `league_hash_image` STRING,
    `season_statistics_type` STRING,
    `league_name` STRING,
    `league_id` INT64,
    `coaches_id` INT64,
    `graphs_id` INT64,
    `round` RECORD,
    `duration` INT64,
    `lineups_id` INT64,
    `class_id` INT64,
    `start_time` TIMESTAMP,
    `class_hash_image` STRING,
    `class_name` STRING,
    `coaches` RECORD,
    `season_name` STRING,
    `home_team_hash_image` STRING,
    `away_team_score` RECORD,
    `attendance` INT64,
    `home_team_score` RECORD,
    `tournament_importance` INT64,
    `arena_hash_image` STRING,
    `home_team_name` STRING,
    `status` RECORD,
    `round_id` INT64,
    `home_team_id` INT64,
    `last_period` STRING,
    `previous_leg_match_id` INT64,
    `tournament_id` INT64,
    `referee_hash_image` STRING,
    `time` STRING,
    `season_id` INT64,
    `referee_name` STRING,
    `away_team_name` STRING,
    `arena_name` STRING,
    `arena_id` INT64,
    `away_team_id` INT64,
    `status_type` STRING,
    `weather_icon` STRING,
    `weather_id` INT64,
    `tournament_name` STRING,
    `weather_main` STRING,
    `name` STRING,
    `away_team_hash_image` STRING,
    `times` RECORD,
    `id` INT64,
    `away_team` RECORD,
    `home_team` RECORD,
    `confirmed` BOOLEAN
)
