CREATE OR REPLACE TABLE `birkbeck-msc-project-422917.extract_layer.all_api_sportdevs_fb_standings` 

(
`tournament_name` STRING,
    `competitors` RECORD,
    `league_hash_image` STRING,
    `league_name` STRING,
    `league_id` INT64,
    `season_id` INT64,
    `type` STRING,
    `tournament_id` INT64,
    `name` STRING,
    `season_name` STRING,
    `id` INT64
)
