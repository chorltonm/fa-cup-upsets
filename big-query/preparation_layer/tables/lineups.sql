CREATE OR REPLACE TABLE `birkbeck-msc-project-422917.preparation_layer.lineups` (
    `lineup_id` INT64,
    `lineup_home_away_flag` STRING,
    `lineup_confirmed` BOOLEAN,
    `lineup_formation` STRING,
    `lineup_player_id` INT64,
    `lineup_player_name` STRING,
    `lineup_position` STRING,
    `lineup_substitute` BOOLEAN,
    `lineup_player_statistics_id` INT64,
    `lineup_players_availability` STRING,
    `lineup_players_availability_reason` STRING,
    `apperance_count` INT64
)
