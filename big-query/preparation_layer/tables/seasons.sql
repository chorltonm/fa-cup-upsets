CREATE OR REPLACE TABLE `birkbeck-msc-project-422917.preparation_layer.seasons` (
    `season_league_id` INT64,
    `season_league_name` STRING,
    `season_id` INT64,
    `season_name` STRING,
    `season_year` STRING,
    `season_start_time` DATE,
    `season_end_time` DATE
)
